what i"ve learned :

=> degree to rotate a second hand of clock in a sec = ((now.getSeconds()/60)*360);

=> to make a required degree of rotation = secondHand.style.transform = `rotate(${secondsDegree}deg)`;

=> call the function with a setinterval of 1000ms --> setInterval(setDate,1000);
